{// variables
var name = "Mihir";
let  age = 24;
const country = "india";
}

{
//Data types
let stringType = "Hello, World"; // string
let numberType = 550; //number
let booleanType = true; // boolean
let undefinedType; //undefined
let nullType = null; //null
}

{
//operators

// Arithmetic Operators: +, -, *, /, %
// Assignment Operators: =, +=, -=, *=, /=
// Comparison Operators: ==, ===, !=, !==, >, <, >=, <=
// Logical Operators: &&, ||, !


let sum = 5+2;
 let isEqual = 10 == "10"; //(it is not very strict )it only compares the value of the data
 let isStrict = 10 === "10"; //(it is very strict) it will check both the data value and the data type 
}

{
    //functions

    function greet(name) {
        return "Hello", + name;
    }

    let message = greet("mihir")
    console.log(message);
}

{
    // conditionals

    let time = 20;

if (time < 12) {
  console.log("Good Morning");
} else if (time < 18) {
  console.log("Good Afternoon");
} else {
  console.log("Good Evening");
}

}

{
    
}